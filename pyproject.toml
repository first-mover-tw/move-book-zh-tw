[project]
name = "move-book"
version = "0.1.0"
description = "The Move Book, Traditional Chinese (Taiwan)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-genai>=1.61.0",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
